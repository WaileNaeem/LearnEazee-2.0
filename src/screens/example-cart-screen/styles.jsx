import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  contentContainer: {
    flex: 1,
    paddingHorizontal: '3%',
    borderColor: 'red',
    borderWidth: 1,
  },
});
export default styles;
